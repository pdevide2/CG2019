/* sp_getColunaFiltro CG_USUARIO */
ALTER PROCEDURE sp_getColunaFiltro
@TABELA VARCHAR(50)
AS
BEGIN

SELECT COLUNA
FROM 
	CG_PARAMETRO_PESQUISA_DINAMICA
WHERE 
	TABELA = @TABELA
	AND COLUNA_FILTRO = 1
ORDER BY 	ORDERCOL


END
