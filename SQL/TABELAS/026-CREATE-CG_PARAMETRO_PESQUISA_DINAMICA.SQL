CREATE TABLE CG_PARAMETRO_PESQUISA_DINAMICA (
TABELA			VARCHAR(50) NOT NULL,
COLUNA			VARCHAR(50) NOT NULL,
IDCOLUNA		INT NOT NULL,
ORDERCOL		INT NOT NULL DEFAULT(0), 	
CHAVE			BIT NOT NULL DEFAULT (0), 	
TIPODADO		VARCHAR(50) NULL,
COLUNA_FILTRO	BIT NOT NULL DEFAULT (0),
APELIDO_COLUNA	VARCHAR(50) NULL,
MOSTRA_COLUNA	BIT NOT NULL DEFAULT(0)
)

GO

ALTER TABLE CG_PARAMETRO_PESQUISA_DINAMICA
ADD PRIMARY KEY (TABELA, COLUNA)
GO

