CREATE TABLE CG_ESTOQUE_CHIP (
ID_CHIP INT NOT NULL PRIMARY KEY,
ESTOQUE INT NOT NULL,
TRANSITO BIT NOT NULL DEFAULT(0),
ID_LOCAL INT NOT NULL DEFAULT(0),
DATAMOV DATETIME NOT NULL DEFAULT (GETDATE()) )